export const API=process.env.NEXT_PUBLIC_API_URL??"http://localhost:8080";export const WEB=process.env.NEXT_PUBLIC_WEB_URL??"http://localhost:3000";export type Route={country:string;city:string;category:string};export async function getRoutes():Promise<Route[]>{try{const r=await fetch(`${API}/api/v1/seo/routes`,{next:{revalidate:86400}});return r.ok?(await r.json()).items:[]}catch{return[]}}export async function getLanding(x:Route){try{const r=await fetch(`${API}/api/v1/seo/${x.country}/${x.city}/${x.category}`,{next:{revalidate:86400}});return r.ok?r.json():null}catch{return null}}export function xmlUrls(urls:string[]){return `<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${urls.map(url=>`<url><loc>${escapeXml(url)}</loc><changefreq>daily</changefreq></url>`).join("")}</urlset>`}export function xmlResponse(body:string){return new Response(body,{headers:{"content-type":"application/xml","cache-control":"public, max-age=86400"}})}function escapeXml(v:string){return v.replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;")}
