export function GET(){const base=process.env.NEXT_PUBLIC_WEB_URL??"http://localhost:3000";return new Response(`# wedjan\n\nwedjan is a three-sided event marketplace for customers, vendors, and event freelancers. Public vendor packages always include real prices. Customers pay 0% booking fees.\n\n## Canonical entities\n- Vendor profiles: ${base}/v/{vendor-slug}\n- Search: ${base}/search?category={category}&city={city}\n- Real event showcases: ${base}/inspiration/{showcase-slug}\n- Category-city guides: ${base}/{country-code}/{city-slug}/{category-slug}\n\nOnly accepted vendor credits appear on public showcases. Reviews, when launched, are limited to verified bookings.\n`,{headers:{"content-type":"text/plain; charset=utf-8","cache-control":"public, max-age=3600"}})}
